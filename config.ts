
export const CONFIG = {
  // Substitua 'https://seu-dominio.com' pela URL real onde seu servidor Node.js está rodando na Hostinger.
  // Se o frontend e backend estiverem no mesmo domínio, você pode usar apenas '/api'
  API_BASE_URL: '/api',
  AUTH_TOKEN_KEY: 'barber_pro_token'
};
